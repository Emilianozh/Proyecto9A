// JS movido desde historial.html
if (window.performance && window.performance.navigation.type === 2) {
	window.location.reload();
}
// No JS necesario actualmente para historial, solo estructura para futuro uso.
